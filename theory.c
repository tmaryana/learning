#include <stdio.h>
//Ещё б понять как это всё работает
int main(void) {
  // Тут будет теория. После двойного слэша, как в начале этой строки, - идёт комментарий кода. Вот так и пишем пояснения.
  printf("Hello!");
  return 0;
}
