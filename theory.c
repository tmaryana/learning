#include <stdio.h>

int main(void) {
  // Тут будет писать теорию. После двойного слэша, как в начале этой строки - идёт комментарий кода. Вот так и пишем пояснения.
  printf("Hello!");
  return 0;
}
